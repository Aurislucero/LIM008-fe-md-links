"use strict";

var fs = require('fs');

var path = require('path');
/**
 * 
 * @param {*} route 
 * @param {*} options
 * @returns 
 */
// export const mdLinks = (route, options) => {
//     return new Promise((resolve, reject) => {
//         resolve([])
//     })
// }