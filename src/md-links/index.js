const fs = require('fs');
const path = require('path');

/**
 * 
 * @param {*} route 
 * @param {*} options
 * @returns 
 */
export const mdLinks = (route, options) => {
    return new Promise((resolve, reject) => {
        resolve([])
    })
}
  
  
  